---
phase: 05-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/routes_static.py
  - encrypt_api.py
  - utils/encryption.py
  - utils/pdf_encryption.py
autonomous: true

must_haves:
  truths:
    - "No unused imports exist in routes/routes_static.py"
    - "No references to non-existent legacy files remain in application code"
    - "All Python files pass linting for unused imports"
  artifacts:
    - path: "routes/routes_static.py"
      provides: "Static routes with clean imports"
    - path: "encrypt_api.py"
      provides: "Main entry point with updated docstring"
    - path: "utils/pdf_encryption.py"
      provides: "PDF encryption with corrected help strings"
  key_links:
    - from: "routes/routes_static.py"
      to: "flask"
      via: "imports"
      pattern: "from flask import"
---

<objective>
Remove unused imports and update legacy file references in application code

Purpose: Eliminate dead code and stale references to files that no longer exist (encrypt_api_new.py, EncTestNewTestF.py)
Output: Clean Python source files with no unused imports or references to deleted files
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean unused imports in routes_static.py</name>
  <files>routes/routes_static.py</files>
  <action>
Remove the following unused imports from routes/routes_static.py:

1. Remove `import requests` (line 7) - not used anywhere in the file
2. Remove `SECRET_KEY, BS4_AVAILABLE` from the routes_common import (line 9) - neither is used

The import line should change from:
```python
from .routes_common import SECRET_KEY, BS4_AVAILABLE
```
to:
```python
# No import needed from routes_common - this file only serves static files
```

If the entire routes_common import becomes empty, remove the import line entirely.
  </action>
  <verify>
Run: `python3 -c "from routes.routes_static import register_static_routes; print('Import OK')"`
Verify no syntax errors or import failures.
  </verify>
  <done>routes_static.py has no unused imports</done>
</task>

<task type="auto">
  <name>Task 2: Update legacy file references in source code</name>
  <files>encrypt_api.py, utils/encryption.py, utils/pdf_encryption.py</files>
  <action>
Update references to legacy files that no longer exist:

1. **encrypt_api.py** (line 5): Change docstring from:
   ```
   Uses the EXACT algorithm from EncTestNewTestF.py
   ```
   to:
   ```
   Uses Feistel cipher encryption (utils.encryption module)
   ```

2. **utils/encryption.py** (line 14): Change comment from:
   ```
   """EXACT copy from EncTestNewTestF.py"""
   ```
   to:
   ```
   """Expand Unicode ligatures to ASCII equivalents"""
   ```

3. **utils/pdf_encryption.py**: Update CLI help strings that reference EncTestNewTestF.py:
   - Line 754: Change `python3 EncTestNewTestF.py` to `python3 -m utils.pdf_encryption`
   - Lines 1215-1221: Update usage examples to use module path instead of old filename

   Example changes:
   ```
   python3 EncTestNewTestF.py input.pdf --secret-key 29202393 output.pdf
   ```
   becomes:
   ```
   python3 -m utils.pdf_encryption input.pdf --secret-key 29202393 output.pdf
   ```
  </action>
  <verify>
Run: `grep -r "EncTestNewTestF\|encrypt_api_new" utils/ routes/ *.py 2>/dev/null | grep -v ".planning"`
Should return empty (no matches outside .planning/).
  </verify>
  <done>No source code references non-existent legacy files</done>
</task>

<task type="auto">
  <name>Task 3: Remove one-time verification script</name>
  <files>verify_logging.py</files>
  <action>
Delete the file `verify_logging.py` from the project root.

This was a one-time verification script created for quick task 003 (logging cleanup verification). It is:
- Not imported by any module
- Not part of the application
- Not needed for ongoing development
- A completed task artifact

Use `git rm` to properly remove from version control:
```bash
git rm verify_logging.py
```
  </action>
  <verify>
Run: `ls -la verify_logging.py 2>&1`
Should return "No such file or directory"
  </verify>
  <done>One-time verification script removed from codebase</done>
</task>

</tasks>

<verification>
1. All Python files import successfully: `python3 -c "import encrypt_api; import routes.registry; print('All imports OK')"`
2. No references to legacy files: `grep -r "EncTestNewTestF\|encrypt_api_new" . --include="*.py" | grep -v ".planning" | grep -v ".git"`
3. Flask server starts: `python3 encrypt_api.py &` (kill after confirming startup)
4. Run test suite: `npx playwright test --reporter=dot`
</verification>

<success_criteria>
- routes_static.py has no unused imports
- No application source files reference EncTestNewTestF.py or encrypt_api_new.py
- verify_logging.py is deleted
- All 29 Playwright tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-cleanup/05-01-SUMMARY.md`
</output>
