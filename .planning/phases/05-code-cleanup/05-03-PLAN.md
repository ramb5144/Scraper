---
phase: 05-code-cleanup
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "All 29 Playwright tests pass after code cleanup"
    - "Flask server starts without import errors"
    - "No regressions from removing unused code"
  artifacts:
    - path: "test-results/"
      provides: "Playwright test results showing all passing"
  key_links:
    - from: "tests/specs/*.spec.ts"
      to: "routes/*.py"
      via: "HTTP requests to Flask server"
      pattern: "page.goto\\("
---

<objective>
Verify all tests pass after code cleanup changes

Purpose: Confirm removing unused imports and legacy references caused no regressions
Output: All 29 Playwright tests passing, confirming Phase 5 code changes are safe
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-code-cleanup/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Flask server starts correctly</name>
  <files></files>
  <action>
1. Kill any existing Flask processes on port 8001:
   ```bash
   lsof -ti:8001 | xargs kill -9 2>/dev/null || true
   ```

2. Start Flask server and verify it starts without import errors:
   ```bash
   cd /Users/tyler/Downloads/cloaktest28.3\ 5\ copy\ 2
   python3 encrypt_api.py &
   sleep 3
   ```

3. Verify server responds:
   ```bash
   curl -s http://localhost:8001/api/health | head -20
   ```
   Should return JSON with status information.

4. Verify key routes work:
   ```bash
   curl -s -o /dev/null -w "%{http_code}" http://localhost:8001/sdk-test
   curl -s -o /dev/null -w "%{http_code}" http://localhost:8001/dynamic-test
   ```
   Both should return 200.
  </action>
  <verify>
All routes return HTTP 200 status codes.
Server startup shows no import errors.
  </verify>
  <done>Flask server starts without errors and routes respond correctly</done>
</task>

<task type="auto">
  <name>Task 2: Run full Playwright test suite</name>
  <files></files>
  <action>
Run all 29 Playwright tests to verify no regressions:

```bash
cd /Users/tyler/Downloads/cloaktest28.3\ 5\ copy\ 2
npx playwright test --reporter=dot
```

Expected output: 29 tests passing (same as before Phase 5 cleanup).

Test categories that should pass:
- Smoke tests (basic page loading)
- Encryption tests (SDK encryption verification)
- Copy/paste tests (plaintext clipboard handling)
- Visual tests (rendering verification)
- Dynamic content tests (content update handling)
  </action>
  <verify>
Test output shows "29 passed" with no failures.
  </verify>
  <done>All 29 Playwright tests pass after code cleanup</done>
</task>

</tasks>

<verification>
1. Flask server starts without import errors
2. All demo routes return HTTP 200
3. All 29 Playwright tests pass
4. No new test failures or warnings
</verification>

<success_criteria>
- Flask server starts successfully with no import errors
- All 29 Playwright tests pass (same as before cleanup)
- No regressions from Phase 5 code changes
- Phase 5 complete and ready for Phase 6
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-cleanup/05-03-SUMMARY.md`
</output>
