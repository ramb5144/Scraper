---
phase: 03-automated-testing-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/specs/visual.spec.ts
  - tests/specs/dynamic.spec.ts
  - tests/pages/dynamic-test-page.ts
  - tests/fixtures/test-page.ts
autonomous: true

must_haves:
  truths:
    - "Visual regression detects gibberish rendering (plaintext + encrypted font)"
    - "Dynamically added content gets encrypted by MutationObserver"
    - "Screenshot baselines exist for key elements"
  artifacts:
    - path: "tests/specs/visual.spec.ts"
      provides: "Screenshot regression tests"
      min_lines: 40
    - path: "tests/specs/dynamic.spec.ts"
      provides: "MutationObserver and dynamic content tests"
      min_lines: 40
    - path: "tests/pages/dynamic-test-page.ts"
      provides: "Page Object for dynamic_test.html"
      exports: ["DynamicTestPage"]
  key_links:
    - from: "tests/specs/visual.spec.ts"
      to: "Playwright snapshots"
      via: "toHaveScreenshot()"
      pattern: "toHaveScreenshot"
    - from: "tests/specs/dynamic.spec.ts"
      to: "cloak-sdk.js MutationObserver"
      via: "DOM mutation verification"
      pattern: "_cloakOriginal"
---

<objective>
Implement visual regression tests to catch gibberish rendering and dynamic content tests to verify MutationObserver encryption works.

Purpose: Detect the FAQ gibberish bug (plaintext rendered with encrypted fonts) via screenshot comparison, and verify dynamic content encryption enables testing of SPA-like behavior.

Output: Visual regression tests with baselines for key elements, dynamic content tests for MutationObserver verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-automated-testing-infrastructure/03-RESEARCH.md
@.planning/phases/03-automated-testing-infrastructure/03-01-SUMMARY.md

# Relevant source files
@sdk_test.html
@templates/dynamic_test.html
@client/cloak-sdk.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create visual regression tests</name>
  <files>tests/specs/visual.spec.ts, tests/fixtures/test-page.ts</files>
  <action>
1. Update tests/fixtures/test-page.ts:
   - Add helper: disableAnimations() - inject CSS to disable all animations/transitions:
     ```typescript
     async disableAnimations() {
       await this.page.addStyleTag({
         content: '*, *::before, *::after { animation-duration: 0s !important; transition-duration: 0s !important; }'
       });
     }
     ```

2. Create tests/specs/visual.spec.ts importing test/expect from fixtures

3. Test: 'h1 renders correctly (not gibberish)'
   - Init SDK
   - Wait for fonts ready
   - Disable animations
   - Take screenshot of h1 element: await expect(page.locator('h1')).toHaveScreenshot('h1-encrypted.png', { maxDiffPixels: 50 })
   - First run creates baseline, subsequent runs compare

4. Test: 'article section renders correctly'
   - Init SDK
   - Wait for fonts
   - Disable animations
   - Screenshot .article or main content section
   - Use maxDiffPixels: 100 for larger element

5. Test: 'status indicator shows success state'
   - Init SDK (which should show success)
   - Screenshot #status element
   - This catches UI state regressions

6. Add beforeEach hook:
   - Disable animations in all visual tests to ensure consistency

Note: First run generates baselines in tests/specs/visual.spec.ts-snapshots/. Commit these to git for CI comparison.
  </action>
  <verify>
- `npm test tests/specs/visual.spec.ts` passes (or creates baselines on first run)
- Snapshot files exist in tests/specs/visual.spec.ts-snapshots/
- Running twice produces no diff (baselines stable)
  </verify>
  <done>Visual regression tests with baselines for h1, article, and status elements</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic content Page Object</name>
  <files>tests/pages/dynamic-test-page.ts, tests/fixtures/test-page.ts</files>
  <action>
1. Create tests/pages/dynamic-test-page.ts with DynamicTestPage class:
   - Constructor takes Page
   - goto(): Navigate to /dynamic_test (or /templates/dynamic_test.html depending on Flask routing)
   - waitForFontsReady(): Same as SdkTestPage
   - waitForSDKInit(): Wait for CloakSDK.getStats().initialized === true via waitForFunction
   - getFAQHeader(): Return h2 containing "Frequently" via locator
   - getNavLinks(): Return nav link elements
   - addDynamicContent(): If page has "add content" button, click it. Otherwise inject content:
     ```typescript
     async addDynamicContent(html: string) {
       await this.page.evaluate((content) => {
         const container = document.querySelector('#dynamic-content') || document.body;
         const div = document.createElement('div');
         div.innerHTML = content;
         container.appendChild(div);
       }, html);
     }
     ```
   - getDynamicContentEncrypted(selector): Get textContent of dynamically added element
   - getDynamicContentPlaintext(selector): Get _cloakOriginal

2. Update tests/fixtures/test-page.ts:
   - Import DynamicTestPage
   - Add dynamicPage fixture that goes to dynamic_test.html and waits for SDK init
  </action>
  <verify>
- File exists: tests/pages/dynamic-test-page.ts
- Fixture added to tests/fixtures/test-page.ts
  </verify>
  <done>DynamicTestPage POM with methods for dynamic content testing</done>
</task>

<task type="auto">
  <name>Task 3: Create dynamic content and MutationObserver tests</name>
  <files>tests/specs/dynamic.spec.ts</files>
  <action>
1. Create tests/specs/dynamic.spec.ts importing test/expect from fixtures

2. Test: 'SDK initializes on dynamic_test page'
   - Use dynamicPage fixture
   - Assert page loads without error
   - Assert SDK stats show initialized: true

3. Test: 'dynamically added content is encrypted'
   - Use dynamicPage fixture
   - Add dynamic content: '<h3 class="dynamic-heading">Breaking News</h3>'
   - Wait 100ms for MutationObserver batch processing (batchDelay: 16ms + buffer)
   - Assert getDynamicContentEncrypted('.dynamic-heading') !== getDynamicContentPlaintext('.dynamic-heading')
   - Assert plaintext === 'Breaking News'

4. Test: 'multiple dynamic additions are all encrypted'
   - Add 3 different pieces of content
   - Wait for processing
   - Assert all 3 are encrypted with correct plaintext

5. Test: 'FAQ header is encrypted (not gibberish)'
   - This tests the known FAQ bug
   - Use dynamicPage fixture
   - Get FAQ h2 element
   - Assert encrypted !== plaintext
   - Assert plaintext contains 'Frequently' (not 'Freqfkntly')
   - If this test fails, it captures the FAQ gibberish bug automatically

6. Test: 'visual check - FAQ header renders correctly'
   - Take screenshot of FAQ header
   - Use maxDiffPixels: 50
   - This would catch gibberish rendering visually
  </action>
  <verify>
- `npm test tests/specs/dynamic.spec.ts` passes
- FAQ tests either pass (bug fixed) or fail with clear message (bug exists)
- Dynamic content encryption verified
  </verify>
  <done>Dynamic content tests verify MutationObserver encryption and FAQ rendering</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
# Run all tests
npm test

# List all tests
npm test -- --list

# Check snapshot baselines exist
ls tests/specs/*.spec.ts-snapshots/

# Run visual tests specifically to generate baselines
npm test tests/specs/visual.spec.ts -- --update-snapshots
```

Expected: All tests pass, baselines generated, FAQ test detects current state of bug.
</verification>

<success_criteria>
- visual.spec.ts has screenshot tests for h1, article, status
- dynamic.spec.ts has tests for MutationObserver encryption
- FAQ tests exist to detect gibberish bug
- Snapshot baselines committed (or ready to commit)
- `npm test` runs full suite successfully
- Test output clearly indicates any failures with trace files for debugging
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-testing-infrastructure/03-03-SUMMARY.md`
</output>
