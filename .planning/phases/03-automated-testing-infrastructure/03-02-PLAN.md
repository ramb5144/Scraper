---
phase: 03-automated-testing-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/specs/encryption.spec.ts
  - tests/specs/copy-paste.spec.ts
  - tests/pages/sdk-test-page.ts
autonomous: true

must_haves:
  truths:
    - "Copy/paste returns original plaintext, not encrypted text"
    - "All visible text nodes are encrypted after SDK init"
    - "Encrypted fonts are applied to encrypted elements"
    - "Position mapping is accurate for h1 and paragraph text"
  artifacts:
    - path: "tests/specs/encryption.spec.ts"
      provides: "Core encryption verification tests"
      min_lines: 50
    - path: "tests/specs/copy-paste.spec.ts"
      provides: "Clipboard and position mapping tests"
      min_lines: 60
  key_links:
    - from: "tests/specs/copy-paste.spec.ts"
      to: "decrypt-interceptor"
      via: "clipboard event interception"
      pattern: "clipboard\\.readText"
    - from: "tests/specs/encryption.spec.ts"
      to: "cloak-sdk.js"
      via: "CloakSDK.getStats() verification"
      pattern: "_cloakOriginal"
---

<objective>
Implement core encryption and copy/paste tests that verify the SDK encrypts text correctly and the decrypt-interceptor returns accurate plaintext on copy.

Purpose: Create automated tests that can detect the two most critical failure modes: (1) text not being encrypted and (2) copy/paste returning wrong text (position mapping bugs).

Output: Test specs for encryption verification and clipboard accuracy that enable autonomous diagnosis of bugs like the FAQ gibberish issue.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-automated-testing-infrastructure/03-RESEARCH.md
@.planning/phases/03-automated-testing-infrastructure/03-01-SUMMARY.md

# Relevant source files
@sdk_test.html
@client/cloak-sdk.js
@client/decrypt-interceptor.js
@client/decrypt/src/position.js
@client/decrypt/src/copy.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create encryption verification tests</name>
  <files>tests/specs/encryption.spec.ts</files>
  <action>
1. Create tests/specs/encryption.spec.ts importing test/expect from fixtures

2. Add describe block: 'Encryption Verification'

3. Test: 'all headings are encrypted'
   - Init SDK
   - For each h1, h2, h3 on page:
     a) Get textContent (encrypted)
     b) Get _cloakOriginal via evaluate
     c) Assert encrypted !== plaintext
     d) Assert plaintext is a real English string (not empty, not symbols)

4. Test: 'paragraph text is encrypted'
   - Init SDK
   - Select first 3 paragraphs
   - Assert each has encrypted !== plaintext

5. Test: 'encrypted fonts are applied'
   - Init SDK
   - Wait for fonts ready
   - Get computed font-family for h1
   - Assert font-family contains 'CloakFont' substring

6. Test: 'script and style elements are not encrypted'
   - Init SDK
   - Evaluate: check that script/style elements don't have _cloakOriginal on text nodes
   - This validates excludeSelectors works

7. Test: 'data-cloak-exclude elements are not encrypted'
   - If sdk_test.html has [data-cloak-exclude] elements, verify they're skipped
   - If not present, skip this test or mark pending
  </action>
  <verify>
- `npm test tests/specs/encryption.spec.ts` passes all tests
- Test output shows 5+ test cases
  </verify>
  <done>Encryption tests verify: all visible text encrypted, fonts applied, exclusions respected</done>
</task>

<task type="auto">
  <name>Task 2: Create copy/paste position mapping tests</name>
  <files>tests/specs/copy-paste.spec.ts, tests/pages/sdk-test-page.ts</files>
  <action>
1. Add helper methods to SdkTestPage:
   - selectText(selector): Use evaluate to create Range, select element's contents, call window.getSelection().addRange()
   - copySelectedText(): Press Ctrl+C (or Meta+C on mac - use page.keyboard.press('ControlOrMeta+c'))
   - readClipboard(): Return navigator.clipboard.readText() via evaluate

2. Create tests/specs/copy-paste.spec.ts importing test/expect from fixtures

3. Test: 'copy h1 returns original plaintext'
   - Init SDK
   - Select h1 text using selectText()
   - Copy using copySelectedText()
   - Read clipboard
   - Assert clipboard === 'The Future of Web Content Protection'

4. Test: 'copy paragraph returns original plaintext'
   - Init SDK
   - Find first paragraph
   - Get expected plaintext via _cloakOriginal
   - Select and copy
   - Assert clipboard matches expected

5. Test: 'partial text selection returns correct portion'
   - Init SDK
   - Use evaluate to select specific character range (e.g., first 10 chars of h1)
   - Copy and verify clipboard has exactly those 10 chars: 'The Future'

6. Test: 'copy across multiple elements returns concatenated plaintext'
   - Init SDK
   - Select a range spanning h1 + first paragraph
   - Copy
   - Assert clipboard contains h1 text followed by paragraph text (with newline/space between)

Note: Context permissions are set in config. If tests fail with permission error, ensure playwright.config.ts has clipboard permissions in use block.
  </action>
  <verify>
- `npm test tests/specs/copy-paste.spec.ts` passes
- Verify in headed mode that selection/copy visibly works: `npm test tests/specs/copy-paste.spec.ts -- --headed`
  </verify>
  <done>Copy/paste tests verify position mapping accuracy for single elements, partial selections, and multi-element ranges</done>
</task>

<task type="auto">
  <name>Task 3: Add edge case tests for position mapping</name>
  <files>tests/specs/copy-paste.spec.ts</files>
  <action>
1. Test: 'code blocks return correct plaintext'
   - If sdk_test.html has <code> or <pre> elements:
     a) Init SDK
     b) Select code element content
     c) Copy and verify plaintext matches original
   - This tests the quick-007/quick-008 fix for code blocks

2. Test: 'text-transform uppercase text returns transformed plaintext'
   - If any element has text-transform: uppercase in css:
     a) Init SDK
     b) Select that element
     c) Copy
     d) Assert clipboard has UPPERCASE text (not original case)
   - Per research: SDK uses transformed text, so copy should match visual

3. Test: 'empty selection returns empty clipboard'
   - Init SDK
   - Clear selection
   - Copy (should be no-op or empty)
   - This is defensive - should not crash

4. If sdk_test.html lacks code blocks or text-transform elements, add a note in test file:
   ```typescript
   test.skip('code blocks return correct plaintext', async () => {
     // TODO: Add code block to sdk_test.html to enable this test
   });
   ```

5. Run full copy-paste test suite to verify all pass
  </action>
  <verify>
- `npm test tests/specs/copy-paste.spec.ts` shows all tests passing or appropriately skipped
- No failures from edge cases
  </verify>
  <done>Edge case tests cover code blocks, text-transform, and empty selection scenarios</done>
</task>

</tasks>

<verification>
Run complete test suite:
```bash
npm test

# Verify in headed mode that copy/paste visually works
npm test tests/specs/copy-paste.spec.ts -- --headed

# Check test count
npm test -- --list
```

Expected: All encryption and copy-paste tests pass, edge cases handled.
</verification>

<success_criteria>
- encryption.spec.ts has 5+ tests covering headings, paragraphs, fonts, exclusions
- copy-paste.spec.ts has 5+ tests covering single, partial, multi-element, edge cases
- All tests pass with `npm test`
- Copy/paste tests correctly use clipboard permissions
- Position mapping accurate for all tested scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-testing-infrastructure/03-02-SUMMARY.md`
</output>
