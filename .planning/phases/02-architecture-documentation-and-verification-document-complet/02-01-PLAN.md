---
phase: 02-architecture-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-architecture-documentation-and-verification-document-complet/flows/complete-flow.md
  - .planning/phases/02-architecture-documentation-and-verification-document-complet/flows/encryption-flow.md
  - .planning/phases/02-architecture-documentation-and-verification-document-complet/flows/decryption-flow.md
autonomous: true

must_haves:
  truths:
    - "Developer can trace text from page load through encryption to DOM update"
    - "Developer can trace copy/paste from selection to clipboard plaintext"
    - "Developer can trace Ctrl+F from query to highlighted results"
  artifacts:
    - path: ".planning/phases/02-architecture-documentation-and-verification-document-complet/flows/complete-flow.md"
      provides: "End-to-end system overview with Mermaid sequence diagram"
      contains: "sequenceDiagram"
    - path: ".planning/phases/02-architecture-documentation-and-verification-document-complet/flows/encryption-flow.md"
      provides: "SDK encryption flow with line references"
      contains: "cloak-sdk.js"
    - path: ".planning/phases/02-architecture-documentation-and-verification-document-complet/flows/decryption-flow.md"
      provides: "Decrypt-interceptor flow for copy/paste/search"
      contains: "position.js"
  key_links:
    - from: "complete-flow.md"
      to: "encryption-flow.md"
      via: "References encryption details"
    - from: "complete-flow.md"
      to: "decryption-flow.md"
      via: "References decryption details"
---

<objective>
Document complete encryption and decryption flows with Mermaid sequence diagrams

Purpose: Enable developers to understand the full text lifecycle from page load through encryption, storage, and copy/paste/search without reading thousands of lines of code.

Output: Three flow documentation files with Mermaid diagrams and code line references
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architecture-documentation-and-verification-document-complet/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md

# Source files to document
@client/cloak-sdk.js
@client/decrypt/src/position.js
@client/decrypt/src/copy.js
@client/decrypt/src/search.js
@client/decrypt/src/selection.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create end-to-end complete flow diagram</name>
  <files>.planning/phases/02-architecture-documentation-and-verification-document-complet/flows/complete-flow.md</files>
  <action>
Create flows/ subdirectory and complete-flow.md documenting the full system lifecycle.

Structure:
1. **Overview section** - Brief text description of Cloak's architecture (3-4 sentences)

2. **Mermaid sequence diagram** showing:
   - Page loads SDK script
   - SDK calls /api/sdk/init (receives secretKey, nonce, charMappings, fontUrl)
   - SDK detects fonts (web fonts, system fonts, generic fonts)
   - SDK requests encrypted fonts from server
   - SDK registers @font-face rules
   - SDK encrypts all text nodes (encryptTextNode loop)
   - SDK uploads plaintext index to R2 (buildAndUploadPlaintext)
   - SDK injects decrypt-interceptor
   - Decrypt-interceptor sets up copy/search listeners
   - User copies text -> decrypt-interceptor gets selection -> API returns plaintext
   - User searches -> API finds matches -> decrypt-interceptor highlights

3. **Critical code locations** table with:
   | Step | File | Lines | Function |

4. **Data flow notes** - what data moves between components at each step

5. **References section** - Links to detailed component flows:
   - "For detailed SDK encryption process, see: encryption-flow.md"
   - "For detailed decrypt-interceptor process, see: decryption-flow.md"

Use the research-recommended Mermaid syntax from 02-RESEARCH.md Pattern 2.
Reference actual line numbers from cloak-sdk.js and decrypt-interceptor source.
  </action>
  <verify>
File exists at .planning/phases/02-architecture-documentation-and-verification-document-complet/flows/complete-flow.md
Contains valid Mermaid sequenceDiagram block
Contains at least 10 participants/messages in diagram
Contains code location table with actual line numbers
  </verify>
  <done>Complete flow diagram documents full lifecycle from SDK init through copy/paste/search</done>
</task>

<task type="auto">
  <name>Task 2: Document SDK encryption flow in detail</name>
  <files>.planning/phases/02-architecture-documentation-and-verification-document-complet/flows/encryption-flow.md</files>
  <action>
Create encryption-flow.md documenting the SDK encryption process in detail.

Structure:
1. **Encryption Overview** - Purpose and approach (Feistel cipher, font-based decryption)

2. **Mermaid sequence diagram** for encryption showing:
   - getTextNodes() TreeWalker traversal
   - shouldExcludeNode() check
   - Zero-width space stripping
   - Block boundary detection (getContainingBlock)
   - Text-transform handling (getTextTransform, applyTextTransform)
   - encryptChar() character mapping
   - plaintextIndex tracking
   - _cloakOriginal storage

3. **Position Tracking Algorithm** section:
   - How totalCharacters is incremented
   - When newline markers (\n) are added for block boundaries
   - plaintextIndex array structure

4. **Text-Transform Handling** section (CRITICAL per research):
   - How CSS text-transform affects encryption
   - When text is transformed before encryption
   - Parent element style.textTransform reset

5. **Code walkthrough** with actual line numbers:
   - getTextNodes(): lines 270-295
   - shouldExcludeNode(): lines 136-242
   - encryptTextNode(): lines 332-411
   - getContainingBlock(): lines 256-262
   - BLOCK_ELEMENTS: lines 245-250
   - getTextTransform()/applyTextTransform(): lines 301-324

Include code snippets for critical functions (not full code, key logic only).
Mark any "MUST MATCH decrypt-interceptor" comments explicitly.
  </action>
  <verify>
File exists at .planning/phases/02-architecture-documentation-and-verification-document-complet/flows/encryption-flow.md
Contains Mermaid diagram
Contains Position Tracking Algorithm section
Contains Text-Transform Handling section
Contains actual line number references to cloak-sdk.js
  </verify>
  <done>Encryption flow documented with position tracking and text-transform handling details</done>
</task>

<task type="auto">
  <name>Task 3: Document decrypt-interceptor flow for copy/paste/search</name>
  <files>.planning/phases/02-architecture-documentation-and-verification-document-complet/flows/decryption-flow.md</files>
  <action>
Create decryption-flow.md documenting how decrypt-interceptor handles copy/paste and search.

Structure:
1. **Overview** - Purpose of decrypt-interceptor (NOT font decryption - that's automatic)

2. **Position Map Building** (CRITICAL - must match SDK):
   - Mermaid diagram of buildTextPositionMap() logic
   - TreeWalker traversal
   - shouldExcludeTextNodeForPositionCalc() (note: different function name than SDK)
   - Zero-width space stripping
   - Block boundary detection with getContainingBlockSkippingHighlights()
   - globalCharIndex tracking

3. **Copy/Paste Flow**:
   - User selects text
   - getSelectionPositions() calculates start/end
   - API call to /api/search/get-text-range
   - Plaintext inserted into clipboard

4. **Search Flow**:
   - User types Ctrl+F
   - Query sent to /api/search/find-matches
   - Server searches R2-stored plaintext
   - Matches returned as {start, end} positions
   - mapPositionsToDOMNodes() maps to DOM
   - Highlights applied with <mark> elements

5. **Code walkthrough** with actual line numbers from position.js:
   - buildTextPositionMap(): lines 417-505
   - shouldExcludeTextNode(): lines 225-283
   - EXCLUDE_SELECTORS: line 217
   - BLOCK_ELEMENTS_SEARCH: lines 295-300
   - getContainingBlockForSearch(): lines 305-311
   - mapPositionsToDOMNodes(): lines 43-114

6. **Search Highlight Interaction** section:
   - How <mark> elements affect position calculations
   - getContainingBlockSkippingHighlights() purpose
   - shouldExcludeTextNodeForPositionCalc() vs shouldExcludeTextNode()

Mark where algorithms MUST match SDK (per MUST MATCH comments in code).
  </action>
  <verify>
File exists at .planning/phases/02-architecture-documentation-and-verification-document-complet/flows/decryption-flow.md
Contains Position Map Building section
Contains Copy/Paste Flow section
Contains Search Flow section
Contains actual line number references to position.js
  </verify>
  <done>Decrypt-interceptor flow documented with position mapping and search highlight handling</done>
</task>

</tasks>

<verification>
All three flow documents exist in flows/ directory:
- ls .planning/phases/02-architecture-documentation-and-verification-document-complet/flows/

Each document contains Mermaid diagrams:
- grep -l "sequenceDiagram" .planning/phases/02-architecture-documentation-and-verification-document-complet/flows/*.md

Documents reference actual code lines (not just "see code"):
- grep -c "line" .planning/phases/02-architecture-documentation-and-verification-document-complet/flows/*.md
</verification>

<success_criteria>
- Three flow documentation files created
- Each contains valid Mermaid sequence diagrams
- Each contains specific code line references
- Text-transform handling documented in encryption flow
- Position mapping documented in decryption flow
- "MUST MATCH" requirements explicitly called out
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-documentation-and-verification-document-complet/02-01-SUMMARY.md`
</output>
