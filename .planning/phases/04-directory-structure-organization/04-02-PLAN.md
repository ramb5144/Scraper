---
phase: 04-directory-structure-organization
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - routes/routes_static.py
  - routes/routes_init.py
autonomous: true

must_haves:
  truths:
    - "All routes reference demos/ directory instead of root or templates/"
    - "send_from_directory calls use demos/ path for demo files"
    - "Flask server starts without errors"
  artifacts:
    - path: "routes/routes_static.py"
      provides: "Updated static file routes pointing to demos/"
      contains: "demos"
  key_links:
    - from: "routes/routes_static.py"
      to: "demos/*.html"
      via: "send_from_directory"
      pattern: "send_from_directory.*demos"
---

<objective>
Update route handlers to serve demo files from the new /demos directory.

Purpose: Ensure Flask routes correctly serve demo files from their new location.
Output: routes_static.py updated with correct paths to demos/ directory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Dependency: Plan 04-01 moved demo HTML files to demos/ directory. Now routes_static.py needs updating to serve from demos/ instead of PROJECT_ROOT or templates/.

Critical: The Playwright tests use these routes:
- /sdk-test -> demos/sdk-test.html (used by encryption.spec.ts, copy-paste.spec.ts, etc.)
- /dynamic-test -> demos/dynamic-test.html (used by dynamic.spec.ts)
- /dynamic-test-plain -> demos/dynamic-test-plain.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update routes_static.py to serve from demos/</name>
  <files>routes/routes_static.py</files>
  <action>
Update routes_static.py to serve demo files from demos/ instead of PROJECT_ROOT or templates/.

Changes needed:

1. Add demos_dir path near the top of register_static_routes():
   ```python
   demos_dir = os.path.join(PROJECT_ROOT, 'demos')
   ```

2. Update these routes to use demos_dir:
   - /test-localhost -> send_from_directory(demos_dir, 'test-localhost.html')
   - /test-localhost-plain -> send_from_directory(demos_dir, 'test-localhost-plain.html')
   - /test-webfonts -> send_from_directory(demos_dir, 'test-localhost-webfonts.html')
   - /test-webfonts-plain -> send_from_directory(demos_dir, 'test-localhost-webfonts-plain.html')
   - /nyt -> send_from_directory(demos_dir, 'nyt.html')
   - /sdk-test -> send_from_directory(demos_dir, 'sdk-test.html')
   - /dynamic-test -> send_from_directory(demos_dir, 'dynamic-test.html')
   - /dynamic-test-plain -> send_from_directory(demos_dir, 'dynamic-test-plain.html')
   - /pdf-test -> send_from_directory(demos_dir, 'pdf-test.html')
   - /stackoverflow-demo -> send_from_directory(demos_dir, 'stackoverflow.html')
   - /test-stackoverflow -> send_from_directory(demos_dir, 'test-stackoverflow.html')
   - /stackoverflow-no-sdk -> send_from_directory(demos_dir, 'stackoverflow-no-sdk.html')

3. Note the filename changes due to kebab-case rename:
   - 'sdk_test.html' -> 'sdk-test.html'
   - 'test_localhost.html' -> 'test-localhost.html'
   - 'test_localhost_plain.html' -> 'test-localhost-plain.html'
   - 'test_localhost_webfonts.html' -> 'test-localhost-webfonts.html'
   - 'test_localhost_webfonts_plain.html' -> 'test-localhost-webfonts-plain.html'
   - 'dynamic_test.html' -> 'dynamic-test.html'
   - 'dynamic_test_plain.html' -> 'dynamic-test-plain.html'
   - 'pdf_test.html' -> 'pdf-test.html'

4. Remove any references to templates_dir for demo files (templates_dir may be used elsewhere for Flask templates, but demo files should use demos_dir).
  </action>
  <verify>
    - `grep -n "demos_dir" routes/routes_static.py` shows the new path variable
    - `grep -n "send_from_directory.*demos" routes/routes_static.py` shows updated routes
    - No grep results for `send_from_directory(PROJECT_ROOT, 'sdk_test` (old pattern)
  </verify>
  <done>
    routes_static.py serves all demo files from demos/ directory
    Filename references updated to kebab-case
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Flask server starts correctly</name>
  <files>None (verification only)</files>
  <action>
Test that Flask server starts without import errors:

1. Run: `cd /path/to/project && FLASK_APP=encrypt_api.py python3 -c "from encrypt_api import app; print('Flask app loaded successfully')"`

2. If there are any import errors, fix them.

This verifies:
- api_routes.py imports work
- routes_static.py changes are valid Python
- All route registrations succeed
  </action>
  <verify>
    Flask app loads without errors
    Output shows "Flask app loaded successfully"
  </verify>
  <done>
    Flask server starts without errors
    Route changes are syntactically correct
  </done>
</task>

</tasks>

<verification>
1. `python3 -c "from encrypt_api import app"` succeeds
2. `grep -c "demos_dir" routes/routes_static.py` returns > 0
3. `grep -c "send_from_directory(PROJECT_ROOT, 'sdk_test" routes/routes_static.py` returns 0
</verification>

<success_criteria>
1. routes_static.py updated with demos_dir path variable
2. All demo file routes use demos_dir instead of PROJECT_ROOT or templates_dir
3. Flask server loads without errors
4. Filename references match kebab-case naming from Plan 04-01
</success_criteria>

<output>
After completion, create `.planning/phases/04-directory-structure-organization/04-02-SUMMARY.md`
</output>
