---
phase: 04-directory-structure-organization
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - routes/registry.py
  - encrypt_api.py
autonomous: true

must_haves:
  truths:
    - "api_routes.py moved into routes/ directory"
    - "Route registration logic accessible from routes/ package"
    - "encrypt_api.py imports from routes/ package"
  artifacts:
    - path: "routes/registry.py"
      provides: "Central route registration module"
      exports: ["register_routes"]
  key_links:
    - from: "encrypt_api.py"
      to: "routes/registry.py"
      via: "from routes.registry import register_routes"
      pattern: "from routes\\.registry import"
---

<objective>
Move api_routes.py into the routes/ directory for consistent organization.

Purpose: All route-related code should live in the routes/ directory.
Output: api_routes.py moved to routes/registry.py, encrypt_api.py import updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Current state: api_routes.py sits at project root but logically belongs in routes/ since it registers all route modules. Moving it improves directory organization.

Critical constraint: encrypt_api.py must continue to work as FLASK_APP=encrypt_api.py for Playwright tests.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move api_routes.py to routes/registry.py</name>
  <files>
    routes/registry.py
  </files>
  <action>
Move api_routes.py to routes/registry.py:

1. Use `git mv api_routes.py routes/registry.py` to preserve history

2. The file content remains the same - it imports from routes.* modules and provides register_routes(app) function

3. Update routes/__init__.py to export register_routes:
   ```python
   # Routes package
   # Individual modules should be imported directly, e.g.:
   #   from routes.routes_encryption import register_encryption_routes
   # Or use the central registry:
   #   from routes.registry import register_routes
   ```
  </action>
  <verify>
    - `ls routes/registry.py` shows the file exists
    - `ls api_routes.py` returns "No such file"
    - `grep "register_routes" routes/registry.py` shows the function
  </verify>
  <done>
    api_routes.py moved to routes/registry.py
    routes/__init__.py updated with documentation
  </done>
</task>

<task type="auto">
  <name>Task 2: Update encrypt_api.py import</name>
  <files>encrypt_api.py</files>
  <action>
Update encrypt_api.py to import from the new location:

Change:
```python
from api_routes import register_routes
```

To:
```python
from routes.registry import register_routes
```

This is the only import change needed since register_routes is a function, not a module.
  </action>
  <verify>
    - `grep "from routes.registry" encrypt_api.py` shows the new import
    - `grep "from api_routes" encrypt_api.py` returns nothing (old import removed)
    - `python3 -c "from encrypt_api import app; print('OK')"` succeeds
  </verify>
  <done>
    encrypt_api.py imports register_routes from routes.registry
    Flask app loads successfully
  </done>
</task>

</tasks>

<verification>
1. `ls routes/registry.py` shows file exists
2. `ls api_routes.py 2>&1` shows "No such file or directory"
3. `python3 -c "from encrypt_api import app"` succeeds without errors
4. `grep "from routes.registry" encrypt_api.py` returns match
</verification>

<success_criteria>
1. api_routes.py no longer exists at project root
2. routes/registry.py contains the register_routes function
3. encrypt_api.py imports from routes.registry
4. Flask app loads without import errors
5. FLASK_APP=encrypt_api.py still works for Playwright
</success_criteria>

<output>
After completion, create `.planning/phases/04-directory-structure-organization/04-03-SUMMARY.md`
</output>
