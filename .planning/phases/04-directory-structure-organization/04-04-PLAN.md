---
phase: 04-directory-structure-organization
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Flask server starts without errors"
    - "All 29 Playwright tests pass"
    - "All demo routes serve correct content"
  artifacts: []
  key_links:
    - from: "Playwright tests"
      to: "/sdk-test, /dynamic-test routes"
      via: "HTTP requests"
      pattern: "baseURL.*localhost:8001"
---

<objective>
Verify all 29 automated tests pass with the new directory structure.

Purpose: Ensure the restructuring did not break any functionality.
Output: Test run confirmation showing all 29 tests passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Dependencies:
- Plan 04-01: Moved demo files to demos/
- Plan 04-02: Updated routes_static.py to serve from demos/
- Plan 04-03: Moved api_routes.py to routes/registry.py

Critical success criteria: All 29 Playwright tests must pass. These tests verify:
- SDK encryption works
- Copy/paste returns plaintext
- Visual rendering is correct
- Dynamic content is handled
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Playwright test suite</name>
  <files>None (verification only)</files>
  <action>
Run the complete Playwright test suite to verify everything works:

```bash
cd /path/to/project && npx playwright test
```

Expected: 29 tests passing

If any tests fail:
1. Check the error message
2. Verify the route path in routes_static.py matches the expected filename
3. Verify the demo file exists in demos/ with correct name
4. Check for any typos in kebab-case naming

Common issues to check:
- sdk-test.html (not sdk_test.html)
- dynamic-test.html (not dynamic_test.html)
- demos_dir path is correct in routes_static.py
  </action>
  <verify>
    Test output shows "29 passed"
    No test failures
  </verify>
  <done>
    All 29 Playwright tests pass
    No regressions from directory restructuring
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify demo routes manually</name>
  <files>None (verification only)</files>
  <action>
Quick manual verification of key routes using curl:

1. Start Flask server (if not running):
   ```bash
   FLASK_APP=encrypt_api.py python3 -m flask run --port 8001 &
   ```

2. Test key routes:
   ```bash
   curl -s -o /dev/null -w "%{http_code}" http://localhost:8001/sdk-test
   # Expected: 200

   curl -s -o /dev/null -w "%{http_code}" http://localhost:8001/dynamic-test
   # Expected: 200

   curl -s -o /dev/null -w "%{http_code}" http://localhost:8001/stackoverflow-demo
   # Expected: 200
   ```

3. Stop the server if started in background

If any route returns 404, check:
- File exists in demos/ with exact name
- routes_static.py uses correct filename
  </action>
  <verify>
    All tested routes return HTTP 200
    No 404 errors for demo pages
  </verify>
  <done>
    Demo routes serve content correctly
    No broken routes after restructuring
  </done>
</task>

</tasks>

<verification>
1. `npx playwright test` shows 29 passing tests
2. `curl -s http://localhost:8001/sdk-test | head -1` shows HTML doctype
3. No 404 errors for any demo route
</verification>

<success_criteria>
1. All 29 Playwright tests pass
2. Flask server starts without errors
3. Demo routes (/sdk-test, /dynamic-test, /stackoverflow-demo) serve correctly
4. No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-directory-structure-organization/04-04-SUMMARY.md`
</output>
