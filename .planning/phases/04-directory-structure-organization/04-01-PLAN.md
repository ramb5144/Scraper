---
phase: 04-directory-structure-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demos/sdk-test.html
  - demos/nyt.html
  - demos/test-localhost.html
  - demos/test-localhost-plain.html
  - demos/test-localhost-webfonts.html
  - demos/test-localhost-webfonts-plain.html
  - demos/dynamic-test.html
  - demos/dynamic-test-plain.html
  - demos/pdf-test.html
  - demos/stackoverflow.html
  - demos/stackoverflow-no-sdk.html
  - demos/stackoverflow-clean.html
  - demos/test-stackoverflow.html
autonomous: true

must_haves:
  truths:
    - "All demo HTML files exist in /demos directory"
    - "Root directory no longer contains scattered demo HTML files"
    - "Templates directory only contains Flask templates (not demos)"
  artifacts:
    - path: "demos/sdk-test.html"
      provides: "Main SDK test page"
    - path: "demos/dynamic-test.html"
      provides: "Dynamic content test page"
    - path: "demos/stackoverflow.html"
      provides: "Stack Overflow demo page"
  key_links:
    - from: "demos/"
      to: "All demo HTML files"
      via: "File move operation"
      pattern: "demos/.*\\.html"
---

<objective>
Consolidate all demo and test HTML files into the /demos directory.

Purpose: Separate static demo content from application code for clearer organization.
Output: All demo HTML files moved to /demos directory, root and templates cleared of demo files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Current state: Demo HTML files are scattered across root level (sdk_test.html, test_localhost*.html, nyt.html) and templates/ directory (dynamic_test.html, stackoverflow.html, etc.). The demos/ directory already exists with only demos/stackoverflow/stackoverflow-closures.html.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move root-level demo HTML files to demos/</name>
  <files>
    demos/sdk-test.html
    demos/nyt.html
    demos/test-localhost.html
    demos/test-localhost-plain.html
    demos/test-localhost-webfonts.html
    demos/test-localhost-webfonts-plain.html
  </files>
  <action>
Move the following demo HTML files from root to demos/:

1. sdk_test.html -> demos/sdk-test.html (rename to kebab-case)
2. nyt.html -> demos/nyt.html
3. test_localhost.html -> demos/test-localhost.html (rename to kebab-case)
4. test_localhost_plain.html -> demos/test-localhost-plain.html
5. test_localhost_webfonts.html -> demos/test-localhost-webfonts.html
6. test_localhost_webfonts_plain.html -> demos/test-localhost-webfonts-plain.html

Use `git mv` for each file to preserve history.

Note: Do NOT move `new stackoverflow.html` - that file has a space in name and appears to be a temporary/working file, not a demo.
  </action>
  <verify>
    `ls demos/*.html` shows all 6 moved files
    `ls *.html` at root shows only `new stackoverflow.html` remaining
  </verify>
  <done>
    Root directory cleared of demo HTML files (except working files)
    All 6 demo files exist in demos/ with kebab-case naming
  </done>
</task>

<task type="auto">
  <name>Task 2: Move templates demo HTML files to demos/</name>
  <files>
    demos/dynamic-test.html
    demos/dynamic-test-plain.html
    demos/pdf-test.html
    demos/stackoverflow.html
    demos/stackoverflow-no-sdk.html
    demos/stackoverflow-clean.html
    demos/test-stackoverflow.html
  </files>
  <action>
Move the following demo HTML files from templates/ to demos/:

1. templates/dynamic_test.html -> demos/dynamic-test.html (rename to kebab-case)
2. templates/dynamic_test_plain.html -> demos/dynamic-test-plain.html
3. templates/pdf_test.html -> demos/pdf-test.html
4. templates/stackoverflow.html -> demos/stackoverflow.html
5. templates/stackoverflow-no-sdk.html -> demos/stackoverflow-no-sdk.html
6. templates/stackoverflow-clean.html -> demos/stackoverflow-clean.html
7. templates/test-stackoverflow.html -> demos/test-stackoverflow.html

Use `git mv` for each file to preserve history.

After moves, the templates/ directory should be empty or contain only Flask templates (currently none remain - templates/ can be deleted if empty).
  </action>
  <verify>
    `ls demos/*.html` shows all demo files including the newly moved ones
    `ls templates/` shows empty or no HTML files
  </verify>
  <done>
    Templates directory cleared of demo files
    All 7 template demo files exist in demos/ with kebab-case naming
    Demos directory now contains 13+ demo HTML files total
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean up empty templates directory</name>
  <files>templates/</files>
  <action>
Check if templates/ directory is empty after moving demo files.

If empty:
- Remove the templates/ directory with `rmdir templates/` or `git rm -r templates/`

If not empty:
- Document what remains (should be nothing after Task 2)
- Leave for future cleanup if needed
  </action>
  <verify>
    `ls templates/` returns "No such file or directory" OR shows only remaining non-demo files
  </verify>
  <done>
    Templates directory either removed (if empty) or documented (if contains non-demo files)
  </done>
</task>

</tasks>

<verification>
1. `ls demos/` shows organized demo files with consistent naming
2. Root directory no longer has sdk_test.html, test_localhost*.html, nyt.html
3. Templates directory either deleted or contains only Flask templates
4. `find . -maxdepth 1 -name "*.html" | grep -v "new stackoverflow"` returns empty
</verification>

<success_criteria>
1. All demo HTML files consolidated in /demos directory
2. Root directory clear of demo files (except temporary working files)
3. Templates directory removed or cleaned
4. File naming consistent (kebab-case)
</success_criteria>

<output>
After completion, create `.planning/phases/04-directory-structure-organization/04-01-SUMMARY.md`
</output>
